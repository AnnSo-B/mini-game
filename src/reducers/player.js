// npm imports

// local imports

// state
const initialState = {
  position: [0, 0],
};

// reducer
export default (state = initialState, action = {}) => {
  switch (action.type) {
    default:
      return state;
  }
};
